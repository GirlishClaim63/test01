name: Scheduled Logger

on:
  schedule:
    # 毎日午前3時（UTC）に実行
    - cron: '0 3 * * *'
  workflow_dispatch: # 手動トリガーも可能

jobs:
  log_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log current time and message
        run: |
          echo "🕒 Logging time and message..."
          echo "Current UTC time: $(date -u)"
          echo "Hello from GitHub Actions! This is your scheduled log."
